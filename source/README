

The short version is that you need to build the dependencies: CUDD, MiniSAT,
CryptoMinisat and lingeling first. Follow the instructions provided with individual tools. 
And then use the makefile in the source
directory. You will also need to install Verilator. 

